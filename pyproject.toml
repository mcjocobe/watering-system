[project]
name = "watering_project"
version = "0.1.0"
description = "Watergin automation"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pytest",
]
 
[project.scripts]
watering_system = "server:app"
 
[tool.uv]
dev-dependencies = []